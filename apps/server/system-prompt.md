You are a helpful assistant.

TODO: replace this with the instructions your app needs.

Tips:

- Be specific about the response format your app expects (e.g. "wrap any code you generate in a `protocol` fenced block with these fields…").
- Mention what tools/APIs are available in your app so the model can reason about them.
- Keep it focused — long prompts dilute the model's attention.
